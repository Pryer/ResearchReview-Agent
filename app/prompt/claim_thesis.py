"""主张语义聚簇提示词。

把同一路线内从各论文摘要抽出的原始主张，归纳为若干"论点"（thesis）。
每个论点可由多篇论文共同支撑，从而让 claim_plan 的 support_level 反映
真实的跨文献证据强度，而不是恒为 single。
"""

CLAIM_THESIS_CLUSTERING_PROMPT = """你是学术综述作者。下面是同一研究路线下从各篇论文中抽取的原始主张，每条都来自单篇论文的原文陈述。

研究路线：{route_name}
研究主题：{topic}

原始主张（claim_index 为稳定编号，务必原样引用）：
{claims_json}

任务：把在**学术含义上讲同一件事**的主张归为一个论点（thesis）。人写综述时不会并列罗列各篇摘要句，而会归纳出"多项研究采用X方法实现Y"这样的论点，再挂上各篇文献。

归并判据（同时满足才可合并）：
1. 断言的对象相同：研究对象、方法类别、分析目标三者中至少两项一致。
2. 断言的内容方向一致：都在说"做了什么/发现了什么/存在什么问题"中的同一件事。
3. 合并后的论点必须能被每一条成员主张各自独立支持，不得出现只有部分成员支持的成分。

严禁合并的情形：
- 只是同属一个宽泛研究主题（例如只是重复主题名称）——这不构成同一论点。
- 方法类别不同（如姿态估计与话语编码），即使应用场景相同。
- 研究对象不同，即使技术手段相同。
- 一条在讲问题、另一条在讲结果——断言方向不同。
- 为了凑数量而合并：宁可保留为单独论点，也不要制造虚假的跨文献共识。

thesis_text 写法要求：
- 用一句陈述句概括该论点，不带引用标记、不带"多项研究""部分工作"等数量措辞（数量措辞由代码按实际证据数决定）。
- 只能包含全部成员主张共有的内容，不得引入任何成员主张之外的新事实、新数值、新结论。
- 保留原始主张中的具体方法名与对象名，不要抽象成空泛表述。

claim_type 从成员主张中取多数；若成员类型不一致且无多数，返回 mixed。

严格返回 JSON（不要输出任何解释文字）：
{{"theses": [{{"thesis_text": "...", "claim_type": "...", "member_indices": [0, 3, 7], "merge_basis": "一句话说明这几条为何是同一论点"}}]}}

约束：
- 每个 claim_index 必须恰好出现在一个 thesis 的 member_indices 中，不得遗漏、不得重复。
- 无法与任何其他主张归并的，单独成为一个只含它自己的 thesis，merge_basis 写"独立论点"。
"""

__all__ = ("CLAIM_THESIS_CLUSTERING_PROMPT",)
