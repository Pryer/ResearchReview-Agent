# Change Log

This file is an append-only record of completed Agent changes. Each entry records
what changed and how it was validated; it is not a replacement for Git history.

## 2026-08-30 18:47:19 +08:00

- **Modified files:** AGENTS.md, README.md, docs/architecture.md,
  docs/research-workflow.md, docs/evidence-model.md, docs/quality-gates.md,
  docs/lessons-learned.md, examples/README.md,
  examples/narrative_review_request.json,
  examples/related_work_request.json,
  examples/unsupported_request.json
- **Root cause/reason:** Documentation responsibilities were mixed together,
  required topic documents and standard examples were missing, and there was no
  unified append-only change record.
- **Behavior change:** AGENTS.md now requires a timestamped change-log entry after
  every completed code change. Documentation responsibilities are separated into
  five topic documents, and examples provide canonical mock-friendly inputs.
- **Tests/validation:** All three example JSON files parsed successfully with
  PowerShell ConvertFrom-Json. No pytest run; this change only affects documents
  and examples.
- **Known limitations:** The change log is Agent-process enforced and is not
  automatically generated by a Git hook or file-system watcher. Existing unrelated
  working-tree changes were not modified.

## 2026-08-30 21:57:06 +08:00

- **Modified files:** app/schemas/deliverable_schema.py, app/agent/writing_plan.py,
  app/agent/nodes/synthesis.py, app/tools/validate_deliverable.py,
  tests/test_route_validator.py, tests/test_writing_plan.py,
  tests/test_writing_architecture.py, tests/test_generation_quality_gate.py
- **Root cause/reason:** Route statistics reported ten or more papers per route while
  the final research-status text still rendered a formal route section from a single
  paper ("本节纳入 1 篇文献", eval_bundle_20260830_200114). Two defects combined: a
  singleton route matching an explicit `required_focus` was exempted from merging in
  `_rebalance_sparse_routes`, so it survived as a formal section; and section evidence
  density was enforced by two separate hardcoded copies that located bodies by exact
  title and counted any citation, so an under-supported or reordered section could
  pass. Section-level density had no contract on the writing plan itself.
- **Behavior change:** `WritingSection` now carries an optional
  `minimum_unique_references` contract, set to 2 for formal research-status route
  sections when the request asks for at least 20 references; it never replaces or
  redistributes the global `required_reference_count`. Singleton routes are always
  merged, including protected focuses; the absorbed focus label is preserved in the
  merged route name when it stays readable and always in the section writing goal, so
  focus coverage is still detected instead of being silently dropped. Citation
  allocation reads the per-section contract first, keeps the previous
  high-citation fallback, and now records deterministic `section_floor_deficits`
  instead of silently keeping a sparse allocation. `validate_deliverable` and
  `validate_final_review_integrity` share one section evaluator that resolves bodies
  by ordinal-stripped title and counts only citations authorized for that section, so
  padding a section with another section's papers no longer satisfies its floor.
  Under-supported sections remain hard errors and continue to be quarantined as a
  best-effort/partial draft rather than reported as success.
- **Tests/validation:** `python -m pytest -q --basetemp=data/_pytest_tmp` →
  919 passed, 3 skipped. The 11 errors seen with the default temp directory are a
  Windows permission problem on `C:\Users\lenovo\AppData\Local\Temp\pytest-of-lenovo`,
  not a code failure. New regressions: unique-owner chain from real overlap
  computation through `validate_routes_node`, `synthesize_themes` and
  `build_writing_plan` with no singleton route section; explicit focuses no longer
  produce single-paper sections while staying covered; a route section citing one
  paper fails both validators; unauthorized citations do not satisfy a section floor;
  allocation reports a section floor deficit when the authorized pool has one paper.
  Replaying the failed artifact through the unified evaluator flags exactly
  "（五）课堂行为分析综述" as `sparse_citations` (1 citation, 74 plain chars).
  The temporary basetemp directory was removed after the run.
- **Known limitations:** No real CNKI/LLM end-to-end run was executed; that path
  needs external services and was not part of this validation. Whether a full live
  run now reaches a fully passing quality gate is therefore unverified — this change
  guarantees that an under-supported route section is merged, or reported and
  quarantined, not that retrieval always yields enough evidence per route. Focus
  coverage still uses token overlap, so a coarse match can mark a focus covered.

## 2026-08-30 23:16:40 +08:00

- **Modified files:** app/tools/validate_deliverable.py, app/agent/nodes/synthesis.py,
  app/agent/writing_plan.py, tests/test_generation_quality_gate.py,
  tests/test_writing_plan.py
- **Root cause/reason:** A live run at 22:41 (eval_bundle_20260830_224148) exposed
  three defects. First, the new section evaluator intersected body citations with the
  plan's `paper_id` authorization, but by the time the final text is audited the
  citation stage has rewritten it into sequential numbers, so every route section
  counted zero citations and all six were reported as under-supported — a false
  positive introduced by the previous entry. Second, citation allocation rotates
  through `dynamic_taxonomy` theme buckets, while route sections can come from
  evidence-requirement or merged routes whose `section_id` is absent from the
  taxonomy; a route could then be selected only once, and unique ownership left no
  donor to move papers from, so one section really did carry a single paper. Third,
  internal coverage labels reached user-visible headings, producing titles such as
  "（一）课堂行为分析相关文献与师生行为相关文献与教学互动相关文献".
- **Behavior change:** Section-floor evaluation now resolves body citations through
  `citation_map` back into `paper_id` space; when no mapping is available it counts
  unique rendered citations and records `identifier_space: rendered` in the finding
  instead of silently passing or silently failing, so authorization stays enforced
  wherever it is meaningful. Citation allocation tops up selection from a section's
  own authorized papers when its floor is unmet, never reusing another section's
  evidence and never lowering the global unique-reference requirement; a genuinely
  under-supplied pool still produces a `section_floor_deficits` entry. Route naming
  strips `相关文献`-style internal suffixes only when prefixed by `相关`, so
  real names like `灰色文献` are untouched.
- **Tests/validation:** `python -m pytest -q --basetemp=data/_pytest_tmp` →
  922 passed, 3 skipped (temp directory removed afterwards). New regressions cover
  rendered numeric citations not false-failing the floor while a truly
  single-citation section still fails, `citation_map` restoring strict per-section
  authorization,
  allocation topping up from the same route when theme buckets are misaligned, and
  internal-label stripping in route names. Replaying the 22:41 artifact through the
  evaluator now flags only "（六）行为编码体系构建" (1 unique citation) instead of
  all six sections. A deterministic 46-card two-deliverable run through
  `generate_deliverables_node` produced four route sections with 7–8 unique
  citations each, no floor failures and no allocation deficits.
- **Known limitations:** Still no real CNKI/LLM end-to-end run; the 22:41 artifact
  was replayed through the validators rather than regenerated. The rendered-space
  fallback counts unique citations without per-section authorization when
  `citation_map` is missing, so a cross-section citation could satisfy a floor in
  that state; unauthorized identifiers are still caught separately by
  `validate_deliverable`. Claim support rate and reference metadata failures seen in
  that run are outside this change's scope and remain open.

## 2026-08-31 21:25:03 +08:00

- **Modified files:** app/schemas/verification_schema.py, app/schemas/paper_schema.py,
  app/tools/verify_claims.py, app/agent/claim_plan.py, app/agent/nodes/synthesis.py,
  app/agent/graph.py, app/agent/nodes/verification.py,
  app/services/research_conversation_service.py, app/tools/extract_paper_card.py,
  app/tools/write_deliverable.py, app/tools/generate_citation.py,
  docs/evidence-model.md, tests/test_verify_claims.py, tests/test_claim_alignment.py,
  tests/test_generation_quality_gate.py, tests/test_agent_graph.py,
  tests/test_research_conversation.py, tests/test_global_evidence_gate.py,
  tests/test_architecture_hardening.py, tests/test_extract.py, tests/test_citation.py,
  tests/test_deliverables.py
- **Root cause/reason:** A live run (job 2f8eff07b2134f209419ffe9fe7f43f5, 59
  references, support rate 0.6338, 19 unsupported claims) exposed three independent
  control-point defects. First, `verify_review_claims` collected every cited paper's
  spans into one candidate list per sentence and unioned their numbers, so "A reaches
  80% and B reaches 95% [p1][p2]" passed as supported even though each paper only
  reported one value; `validate_claim_citation_consistency` compounded this by treating
  the whole matched route's evidence as the authorization set for a single claim.
  Second, the post-generation gate copied the failing draft into `quarantined_draft`
  but left `review`/`body` intact, derived `partial_success` from `bool(draft)` alone,
  and `_assemble_answer` then re-published that quarantined draft, so a 63% support
  rate still returned a formal-looking review. Third, writer input carried no authors
  while the prompt demanded a first-author surname, `_dict_to_card` let LLM output
  overwrite title/authors/venue/DOI/URL, and `citation_check_node` used
  `{**paper, **card}` so a card value could overwrite authoritative retrieval
  metadata — the path that produced "Feng" for a Shuai Ma paper.
- **Behavior change:** Verification now splits each sentence into atomic units bound
  to a single cited paper: clause boundaries produce local binding, and a multi-citation
  clause requires every cited paper to support the complete clause independently, so
  numbers, strong language, access level and LLM entailment can no longer be assembled
  across papers. `ClaimEvidenceResult` gained an optional `atomic_claims` list
  (default empty, old reports still deserialize) and a sentence is supported only when
  all atomic units are. Claim-citation consistency resolves the best matching
  `claim_id` and authorizes only that claim's evidence. The quality gate now carries
  `draft_available`, `draft_released` and `draft_disposition`; an unauthorized
  post-generation failure quarantines the draft, clears `body`, returns only the
  blocking reason, is reported as `blocked`, and enters the session quality-decision
  flow, while an explicit best-effort acceptance keeps the previous warning-banner
  release and `partial` status. `_build_output` enforces this at the publication
  boundary so `answer`/`body`/`related_work`/`introduction` cannot fall back to
  `review`. `PublicationStatus` was added and threaded through
  `PaperMetadata`/`PaperCrawlMetadata`/`PaperCard`; `EvidenceSpan` gained
  `provider`/`source_url`; authors, year, venue, DOI and publication status each get
  their own metadata span and `field_evidence` entry without becoming content claims;
  the card extractor no longer accepts LLM values for any bibliographic field and no
  longer substitutes an LLM year for a missing one; writer input now carries authors,
  venue, DOI, publication and peer-review status alongside authorized claim evidence;
  citation assembly keeps retrieval metadata authoritative; and reference validation
  normalizes DOIs before duplicate detection, reports unknown publication status as a
  warning, and blocks retracted/withdrawn references from ordinary factual use.
- **Tests/validation:** `python -m pytest -q --basetemp=data/_pytest_tmp_full` →
  937 passed, 3 skipped (temp directories removed afterwards). New regressions: two
  papers' numbers cannot be unioned into support; clause-local citations bind to their
  own paper only; each LLM entailment payload item carries one atomic claim and only
  its own evidence; a sibling claim's evidence in the same route no longer authorizes a
  citation; an unauthorized failed draft is quarantined with an empty `body` and a
  sentinel that appears nowhere in the public output; an accepted best-effort draft is
  still released as `partial`; a global-evidence-only `partial` keeps its body; LLM
  forgery of title/authors/venue/DOI is rejected; bibliographic fields each have
  metadata evidence; unknown publication status stays `unknown`; DOI URL/`doi:` forms
  are detected as duplicates; unknown status warns while retracted blocks; and writer
  input is asserted to include the authoritative bibliographic fields.
- **Known limitations:** No real CNKI/LLM end-to-end run was executed; this change was
  validated by unit and integration tests only, so whether a live run now reaches a
  fully passing gate is unverified. Source clients still do not populate
  `publication_status` from Crossref/OpenAlex/S2/CNKI payloads, so most live records
  will remain `unknown` and only produce a warning until that collection work lands;
  the same applies to `EvidenceSpan.provider` for non-metadata spans. Atomic splitting
  is deterministic (clause boundaries plus per-citation expansion) and does not attempt
  semantic decomposition of a single clause, so an internally compound clause with one
  citation is still verified as one unit. Previously persisted sessions are not
  migrated: historical snapshots saved under the old `partial_success` semantics may
  still contain their failed drafts.

## 2026-08-31 23:06:35 +08:00

- **Modified files:** app/agent/route_targets.py (new), app/core/config.py,
  app/schemas/recovery_schema.py, app/agent/evidence_recovery.py,
  app/agent/recovery_loop.py, app/agent/nodes/base.py, app/agent/nodes/retrieval.py,
  app/agent/nodes/synthesis.py, app/agent/state.py, app/agent/graph.py,
  app/tools/rank_papers.py, docs/quality-gates.md,
  tests/test_route_targets.py (new), tests/test_route_recovery_gold.py,
  tests/test_evidence_recovery.py, tests/test_rank.py, tests/test_search.py,
  tests/test_generation_quality_gate.py
- **Root cause/reason:** A route whose final paper count was too low could not get a
  targeted top-up. The `enable_evidence_recovery` loop diagnosed which routes were
  short and asked the LLM for new queries, but every downstream step was global:
  `decide_recovery` merged all routes' suggested queries and took the top
  `max_search_keywords`, so a route with a large deficit could receive zero queries;
  recovery queries were appended after all `keyword_batches` in
  `_select_batch_first_keywords`, so an unfinished batch pushed them out of the round
  entirely; `rank_papers` had no branch quota, so a paper recalled for one route could
  be cut at the global `top_k` before `fetch_detail` ever saw it; convergence used a
  global `count_new_relevant_evidence`, so three papers landing on an already-satisfied
  route counted as progress while the short route stayed at zero;
  `_deterministic_route_gaps` built its route map only from
  `provisional_framework.provisional_routes`, so `_split_oversized_routes` children
  (`{parent}_S{i}`) and post-scope-revision routes were never diagnosed; and
  `_fallback_route_queries` reused the route's first-round queries, whose novelty falls
  below `evidence_recovery_min_query_novelty`, so without an LLM the loop degraded
  immediately. `route_min_core_evidence` was also doing double duty as both the
  sufficiency threshold and the implicit top-up goal.
- **Behavior change:** New `app/agent/route_targets.py` derives a per-route core
  evidence target from the deliverable type and the user's requested reference count:
  `research_status` shares the requested count across active routes clamped to
  `[route_recovery_target_min, route_recovery_target_max]`; `related_work` adds
  `route_recovery_competing_work_bonus` for routes carrying competing-work comparison;
  `narrative_review` additionally tracks year-span diversity as a separate deficit that
  paper count cannot satisfy; any other deliverable (including `research_background`,
  which organizes by argumentative role) gets no extra target and keeps prior behavior.
  `RouteEvidenceGap` now carries `core_evidence_count` / `target_core_evidence` /
  `core_evidence_deficit` / `diversity_deficit`, `RecoveryDecision` carries
  `route_targets` / `route_query_allocation`, and `RecoveryHistoryEntry` carries
  `route_progress`; all default so old persisted state still loads. Gap diagnosis now
  builds its route map from validated ∪ provisional routes, treats a positive deficit
  as a gap on its own, passes the per-route targets into the diagnosis prompt as scale
  guidance (the LLM still cannot emit an action or rewrite a target), and
  regenerates route queries deterministically from core-concept subset combinations
  when the first-round queries are not novel enough, so the loop no longer stalls
  without an LLM. `decide_recovery` allocates the query budget by
  round-robin over deficit-sorted routes so every eligible route gets at least one
  query within budget. `_select_batch_first_keywords` reserves up to half the round's
  slots for `targeted_recovery` branches. `rank_papers` / `deduplicate_and_rank` accept
  `branch_minimums` and apply it in both the language-branched and single-branch paths,
  sized from the current round's summed deficit; it only reallocates `top_k` slots and
  never relaxes a hard filter, scope filter, or semantic screen. Convergence is now
  per-route: recovery stops as `NOT_REQUIRED` only when no route is short of its target,
  and marginal-gain exhaustion counts only new evidence that actually landed on a
  deficit route. `route_recovery_progress` records target / core_before / core_after /
  new_relevant / attempts per route while `route_recovery_attempts` keeps its old
  meaning for the attempt cap. Routes still short after recovery are written to
  `route_evidence_deficits`, merged for writing exactly as before, and surfaced as a
  non-blocking `route_evidence_target_not_met` quality-gate warning that also carries
  the previously unconsumed `section_floor_deficits`.
- **Tests/validation:** `python -m pytest -q --basetemp=data/_pt_full3` →
  954 passed, 3 skipped (temp directory removed afterwards). New regressions: rr14
  (deterministic concept-subset regeneration produces a novel-enough query without an
  LLM); plus those from the first validation round below.
- **Tests/validation (earlier round, before the deterministic query fallback):**
  `python -m pytest -q --basetemp=data/_pt_full2` →
  953 passed, 3 skipped (temp directory removed afterwards). New regressions: seven
  cases in `tests/test_route_targets.py` covering per-deliverable target derivation,
  the competing-work bonus, split sub-route coverage, the small-request floor, and
  narrative-review year-span deficits; gold cases rr09 (route below derived target
  enters the gap report with correct target/deficit), rr10 (split sub-route gap is no
  longer missed), rr11 (three deficit routes each receive at least one query);
  `tests/test_evidence_recovery.py` asserts that evidence landing only on an
  already-satisfied route is not treated as progress and that the short route is
  recorded in `route_evidence_deficits`; `tests/test_rank.py` asserts a
  `branch_minimums` quota lifts targeted recall into `top_k` while a scope-filtered
  recovery paper is still rejected; `tests/test_search.py` asserts recovery queries are
  reserved ahead of batches and that the reservation never takes all slots;
  `tests/test_generation_quality_gate.py` asserts an unmet route target produces a
  warning rather than a blocking issue and the body is still delivered.
- **Known limitations:** No real CNKI/LLM end-to-end run; whether a live run actually
  reaches each route's derived target is unverified, and this change only guarantees
  that the attempt is made per route and that any remaining shortfall is reported
  instead of hidden. Competing-work detection reads existing role labels and route
  names, so a competing-work route with neither signal falls back to the shared target.
  `branch_minimums` is sized from the current round's total deficit rather than
  per-route, so with several simultaneous deficit routes the quota is shared. Research
  background still has no role-based top-up: a missing `limitations` or `challenges`
  argument cannot trigger targeted retrieval, which is the next step if that gap
  matters. The diversity deficit is recorded but does not yet drive query generation.

## 2026-09-02 23:52:41 +08:00

- **Modified files:** app/services/research_conversation_service.py,
  tests/test_research_conversation.py
- **Root cause/reason:** A real 2026-09-01 run (「调研近三年课堂行为分析论文，生成研究背景
  和研究现状，引用论文不少于 40 篇」) delivered a self-contradicting draft. The user answered
  the quality clarification with 「基于现有证据保守重写」, which `_parse_quality_decision`
  matched as `force_generate` because a bare 「写」 candidate sat ahead of the conservative-rewrite
  candidate. `force_generate` then wrote a lowered `required_reference_count` (32 → 25) on both
  the editable state and `research_request`, so `_apply_final_quality_gate` recorded
  `final_requirement_met=true` and the user's explicit 40-paper requirement vanished without a
  trace. `original_required_reference_count` is written but never read anywhere in the repo, so
  the lowering was also irreversible. Separately, the clarification wording, the parser's return
  values and the dispatcher's `if decision["action"] == ...` comparisons were three independently
  hard-coded vocabularies with no shared enumeration, which is the structural reason the system
  offered options it could not parse back.
- **Behavior change:** `force_generate` no longer touches `required_reference_count` or
  `max_papers`; it only sets the degrade markers (`best_effort_generation`,
  `allow_unvalidated_taxonomy`, `quality_recovery_attempts`). Lowering the requirement is now
  exclusive to the `accept_available` branch, where the user has explicitly authorized a specific
  count. A new module-level `_QUALITY_DECISION_OPTIONS` / `_QUALITY_DECISION_ORDER` /
  `_quality_option_label()` triple is the single source of vocabulary: `_quality_clarification`
  composes every option label from it and `_parse_quality_decision` matches against the same list
  in the same order, so traversal order *is* parse priority. The conservative-rewrite candidate
  was moved ahead of `force_generate` and widened to
  `保守重写|重新保守生成|保守生成|重新生成|重新写|重写`; the `force_generate` regex itself is
  byte-identical to before, so 「基于现有证据直接写」 still resolves to `force_generate`.
  `regenerate_existing` still routes to `editable["conservative_regeneration"]=True`, which forces
  `llm=None` and the deterministic `render_fallback` — no LLM rewrite spend for a request that
  asked for a conservative one.
- **Tests/validation:** `python -m pytest tests/test_research_conversation.py
  tests/test_output_quality_fixes.py -q --basetemp=.pytest_tmp` → 108 passed. New regressions:
  `test_force_generate_keeps_original_reference_requirement` (requirement stays at its original
  value and the gate reports `minimum_cited_references_not_met`),
  `test_quality_decision_answers_map_to_intended_actions` (parametrized over six real answers
  including the three that previously mis-resolved), and
  `test_every_clarification_option_label_is_parsable`, a structural guard that renders all five
  gate variants and asserts every offered label parses to a non-`None` action — this is the
  regression fence for the shared-vocabulary change.
- **Known limitations:** Sessions that previously looked like they met the requirement will now
  land in `degraded` instead of `partial`. That is the intended behavior (a gate must change system
  status, not just warn) but it is a user-visible change for anyone accustomed to the silent
  lowering. `original_required_reference_count` is still written by `accept_available` and still
  read by nothing; it is kept as an audit trail for the authorized path only.

## 2026-09-02 23:52:41 +08:00

- **Modified files:** app/agent/nodes/verification.py, app/prompt/claim_repair.py (new),
  app/tools/validate_deliverable.py, app/agent/nodes/synthesis.py, app/core/config.py,
  app/schemas/deliverable_schema.py, tests/test_verify_claims.py,
  tests/test_generation_quality_gate.py
- **Root cause/reason:** The claim-repair stage made the draft *worse* and then reported success.
  Three independent defects: (1) the rewrite acceptance test was an OR —
  `support_rate >= old or unsupported < old` — whose first branch admits a zero-improvement batch,
  which is exactly what landed (`rewritten_count: 7` while unsupported claims stayed at 22 and the
  support rate fell 47.1% → 36.1%); (2) the per-sentence guard `req_cits <= rev_cits` is trivially
  true when the original sentence cites nothing, so a rewrite could invent `[1][2]` provenance out
  of thin air; (3) the rewrite prompt only said "preserve every citation marker" (preserve ≠ do not
  add) and said nothing about restating neighbouring sentences, which is how the verbatim duplicate
  paragraphs appeared. The duplicate-sentence gate was not missing — `_find_duplicate_sentences`
  already existed and already fed `deliverable_structure_invalid` — but `validate_deliverable` only
  runs at generation and citation-backfill time, both *before* `verify_claims_node` rewrites the
  body, and the post-rewrite final audit `validate_final_review_integrity` checked everything
  except duplicates. Finally, `claim_citation_consistency` was computed in `graph.py` and consumed
  only by diagnostics and the result payload, so the observed "9/43 sentences have mismatched
  citations" neither blocked nor degraded anything.
- **Behavior change:** Acceptance is now strict improvement — the batch is persisted only when
  `unsupported` actually drops, otherwise `claim_repairs["strategy"]` becomes
  `section_rewrite_required` and the original text stands. The per-sentence guard is
  `req_cits == rev_cits` (no citation may be lost *or* added) plus a
  `find_similar_sentence(rev, rendered, exclude=target_orig)` check that rejects a rewrite which
  restates an existing sentence; both log a rejection reason. The prompt moved to
  `app/prompt/claim_repair.py` and gained explicit prohibitions on adding citations and on
  restating context. `validate_final_review_integrity` now calls the existing
  `_find_duplicate_sentences` and appends 「最终正文存在重复或高度相似句子」 plus a
  `duplicate_sentence_count` metric, which the gate already turns into the blocking
  `final_text_integrity_not_met`. The similarity rule was extracted into `_is_duplicate_pair` and
  is shared by `_find_duplicate_sentences` and the new public `find_similar_sentence`, so the
  rewrite-time guard and the final audit cannot drift apart. `_apply_final_quality_gate` gained a
  claim-citation consistency code following the established two-tier pattern: below
  `policy.claim_citation_consistency_rate` (new, default 0.80) it is a blocking
  `claim_citation_consistency_not_met`, at or above it a degrading
  `claim_citation_consistency_low` warning. The dead `require_citation_consistency` flag was
  deleted from `ValidationConstraints` (repo-wide grep: zero consumers).
- **Tests/validation:** `python -m pytest tests/test_verify_claims.py
  tests/test_generation_quality_gate.py -q --basetemp=.pytest_tmp` → 86 passed. New regressions:
  `test_rewrite_cannot_add_citations_to_an_uncited_sentence`,
  `test_rewrite_is_rejected_when_it_restates_a_neighbour_sentence`,
  `test_zero_improvement_rewrite_batch_is_not_persisted`,
  `test_final_integrity_rejects_duplicate_sentences_from_claim_rewrite`,
  `test_claim_citation_mismatch_below_threshold_blocks_delivery`,
  `test_claim_citation_mismatch_above_threshold_is_downgraded_to_warning`,
  `test_best_effort_generation_still_reports_cited_reference_shortfall`.
- **Known limitations:** Connecting the duplicate check to the final audit widens the blocking
  surface — drafts that used to ship with repeated sentences will now be quarantined. That is the
  intended consequence, not a regression. The 0.80 consistency threshold is a judgement call
  calibrated against the observed 34/43 (0.791, blocks) and 40/44 (0.909, degrades); it is
  configurable but not yet empirically tuned. No real CNKI/LLM end-to-end run.

## 2026-09-02 23:52:41 +08:00

- **Modified files:** app/agent/writing_plan.py, app/agent/state.py,
  app/agent/nodes/synthesis.py, app/agent/graph.py, tests/test_writing_plan.py,
  tests/test_writing_architecture.py
- **Root cause/reason:** Nearly half the retrieved evidence never reached the draft. Two loss
  points, both silent. (1) `_rebalance_sparse_routes` ended with a bare `merged = merged[:max_routes]`
  slice and branch 1 of `_merge_and_select_themes` ended its selection loops with `break` plus
  `selected_routes[:max_routes]`; with `max_routes=3` (derived from
  `evidence_paper_count // _MIN_PAPERS_PER_SUBSECTION`) the two overflow routes disappeared along
  with their `paper_ids` — the direct source of 5 KEEP routes landing as 2 subsections and 45
  evidence cards yielding only 25 citations. (2) `_absorb_focus_label`'s guard counted absorbed
  labels and characters (`len(current) + len(source_name) + 1 <= 30`) but never counted the 「与」
  segments already inside `current`; since `_semantic_route_name` itself emits two-segment names
  that are not counted as absorptions, a single absorption could chain four segments — the observed
  「（一）编码应用与实证研究与技术辅助编码与多模态融合」 is 21 characters and sailed through.
  Neither event was observable: the step output recorded only `len(plans)`.
- **Behavior change:** Overflow is no longer dropped. Both loss points now route the surplus
  through `_merge_overflow_route`, which scores candidates by the already-proven
  `_tokens(_route_semantic_text(...))` intersection, merges with `_merge_theme_records` (which now
  also unions `evidence_requirement_ids`, removing a duplicated union at the call site) and
  preserves the label via `_absorb_focus_label`. The title guard now counts segments:
  `current.count("与") + source_name.count("与") <= 1`, i.e. at most three segments; the character
  cap is retained. A focus that no longer fits the title is recorded in `protected_focus_labels`,
  which `_theme_section` writes into the section's writing purpose and `_evaluate_focus_coverage`
  still counts as covered — the writing obligation and the evidence survive, only the heading does
  not. Every absorption is appended to the new `state["route_merge_diagnostics"]`
  (reason / merged_route / target_route / migrated_paper_count) with reasons
  `sparse_single_paper_route`, `subsection_quota_overflow` and `theme_similarity_merge`; the
  generation node resets the list per run, `_finalize` accumulates across deliverables, the field
  is carried in `research_state` and rolled back with `writing_plans`, and it is surfaced in the
  `generate_deliverables` step output. The duplicated `theme_text` closure and the duplicated
  `required_focuses` computation were folded into the module-level `_route_semantic_text` and a
  single hoisted binding.
- **Tests/validation:** `python -m pytest tests/test_writing_plan.py
  tests/test_writing_architecture.py -q --basetemp=.pytest_tmp` → 30 passed;
  `tests/test_deliverables.py tests/test_generation_quality_gate.py tests/test_evidence_roles.py
  tests/test_route_validator.py tests/test_verify_claims.py` → 131 passed. New regressions:
  `test_absorb_focus_label_caps_heading_at_three_segments`,
  `test_overflow_routes_merge_instead_of_dropping_their_evidence` (5 routes × 2 papers under a
  3-subsection quota → 3 sections and every route paper still covered — this is the 45 → 25 fence),
  `test_route_merge_diagnostics_records_every_absorption`.
  `test_research_status_reserves_routes_for_explicit_pipeline_focuses` was updated: it previously
  asserted a merged focus appears in a *visible title*, which is exactly the four-segment chaining
  D1 removes; it now asserts the three-segment cap on every section and that both merged focuses
  survive in the planned text (title + purpose).
- **Known limitations:** Merging overflow into a surviving route raises that section's evidence
  density and thematic span; if a merged section starts reading as a mixed-topic grab bag, the
  fallback is to migrate `paper_ids` without the arguments, at the cost of returning to
  bibliographic listing. Partner choice is token-overlap scoring, so a route with no lexical overlap
  with any survivor lands on the largest route regardless of topic. No real CNKI/LLM end-to-end run.

## 2026-09-03 01:10:51 +08:00

- **Modified files:** app/agent/slot_extractor.py, app/agent/state.py,
  app/agent/nodes/retrieval.py, app/agent/nodes/verification.py, app/agent/graph.py,
  app/tools/rank_papers.py, app/tools/paper_rerank.py, app/agent/deliverable_router.py,
  app/tools/cluster_papers.py, app/agent/nodes/synthesis.py,
  tests/test_retrieval_pool_target.py (new), tests/test_rank.py,
  tests/test_generation_quality_gate.py, tests/test_slot_extractor.py
- **Root cause/reason:** The evidence pool was structurally capped below the explicit reference
  requirement, and the resulting shortfall was papered over with papers no LLM had ever screened.
  Four links in one chain, observed on 2026-09-01 (40 references requested, 1101 accumulated
  candidates, 25 cited). (1) `_derive_count_targets` used a 0.60 safety margin for explicit
  counts, so `retrieval_target = generation_limit = 40 + ceil(40*0.60) = 64` clamped any larger
  pool target back to 64. (2) The 50% reserve was applied to the *increment*, not the absolute
  target: first round `ceil(40*1.5)=60`, incremental round `required_to_fetch = max(1, 40-30) = 10`
  → `ceil(10*1.5)=15`, so the pool target shrank from 60 to 15. `evidence_pool_target` was a local
  variable that was never persisted, and no yield metric existed anywhere in state — the only
  related setting was the static `evidence_pool_reserve_ratio=0.5`. (3) `rank_papers` truncated to
  `top_k=64` and discarded the tail, so `llm_rerank_papers` received exactly those 64,
  `candidates == papers`, and the backfill reserve computed as `64 - 30 - 34 = 0` — structurally
  empty. With 34 of 64 excluded there was no way to refill and no code path could look past the
  truncation. (4) `rule_screened_reserve` papers (backfilled by rule score,
  `_screening_confidence=0.0`, never seen by the LLM) were free to become evidence cards and be
  cited, so the gap was silently filled and the gate recorded `final_requirement_met=true`.
  Branched-mode diagnostics made all of this invisible: `rank_node` replaced the whole
  `rule_diagnostics` dict with `{mode, input_count, branch_stats, zh_ratio, zh_ratio_reason}`,
  keeping only a hand-copied `passed_hard_filters` per branch, and branch rerank diagnostics were
  written to locals that were never merged, so the step emitted `llm_rerank: {}`. Investigation had
  to be done by grepping INFO log lines.
- **Behavior change:** (C1) The explicit-count margin ratio is 0.80, so 40 references →
  `retrieval_target = generation_limit = 72`; the `min(40, ...)` margin cap is retained, so a 200
  reference request still yields 240. (C2) `evidence_pool_target` and `evidence_yield` are now
  persisted state fields carried in `research_state` and deliberately kept out of
  `_GENERATION_PRODUCT_KEYS`, so an incremental-retrieval reset of the writing products still
  retains them for the next `fetch_detail`. `evidence_yield_report` records the two loss segments
  separately — `evidence_availability_rate` (usable/cards, 0.711 observed) and
  `citation_realization_rate` (cited/usable, 0.781 observed) — plus `end_to_end_rate` (0.556); it
  is written in `citation_check_node`, the only point where both numbers are final.
  `absolute_evidence_pool_target` derives the target as `ceil(required / yield)`, preferring the
  session's observed end-to-end rate and falling back to `1/(1+reserve_ratio)` (≈0.667) when there
  is no observation, floored at 0.20 so one anomalous run cannot blow the pool up.
  `fetch_detail_node` takes `max(absolute target, increment-derived target)` and persists it, so a
  small increment can no longer shrink the reserve. (C3) `rank_papers` / `deduplicate_and_rank`
  gained `reserve_k`, which appends a rule-eligible tail beyond the `top_k` main window *without
  moving the window*, so `source_minimums` / `branch_minimums` quotas — whose base is `top_k` and
  whose reserve source is `scored[top_k:]` — keep their semantics. `llm_rerank_papers` computes
  `reserve_target = min(top_k, max(minimum_required, round(minimum_required*1.5)))` and, when the
  retained count is still below it after the last batch, pulls the next unscreened papers in rule
  score order and keeps screening until the target is met, the list is exhausted, or
  `rerank_candidate_max` (120) is reached; it stops early when a whole batch retains nothing,
  because papers are already in descending rule score so deeper tails are strictly worse.
  High-confidence exclusions are still never backfilled. The branched path's `minimum_required=0`
  was corrected to the real requirement (dead code in production, since `rank_node` is always
  called with `llm=None`, but wrong on its face). (C4) `RULE_SCREENED_RESERVE` is a single module
  constant in `paper_rerank.py`, compared by the producer (reserve backfill) and by all consumers
  (`cluster_papers.infer_evidence_role`, `synthesis._infer_evidence_role`, and the new gate).
  `deliverable_router._unconfirmed_reference_ids` reads the marker from `paper_details` — the only
  carrier that survives, since `PaperCard` construction drops underscore keys — and `usable_ids`
  excludes those IDs, so `usable_reference_count`, `minimum_references_not_met` and
  `citation_eligible_paper_ids` reflect only LLM-confirmed papers and a shortfall is reported
  instead of padded. `citation_check_node`'s `final_requirement_met` was deliberately left alone:
  per AGENTS.md rule 10 a reserve paper that ends up genuinely cited does count there; C4 tightens
  the pre-writing readiness prediction, not the post-hoc count. (C5) Branched mode now emits
  `zh_filter` / `en_filter` in full plus summed top-level counters via
  `_merge_branch_filter_diagnostics` (counters summed, `filtered_by_stage` / `filter_reasons`
  accumulated key-wise, examples capped at the same 12 as the single-branch path, soft quotas taken
  from either branch rather than summed since both branches receive the same quota); branch rerank
  diagnostics land in `llm_rerank.branches`; `_promote_rerank_diagnostics` lifts seven rerank
  counters to first-class rank-step fields; and a branch rerank failure now records
  `mode="rule_order_fallback"` plus the error instead of only logging it. `filter_reasons` was
  re-wired inside `_record_filter_diagnostic`: it was an orphaned field, read in `retrieval.py` but
  written nowhere in `app/`, so it had been `{}` in both modes; historical bundles
  (`data/classroom_behavior_e2e_result.json`, `docs/react_workflow_example.md`) confirm it used to
  be a live reason→count map, and stage granularity alone cannot answer which concept group or
  exclusion term filtered how many.
- **Tests/validation:** `python -m pytest tests/test_generation_quality_gate.py tests/test_rank.py
  tests/test_retrieval_pool_target.py -q --basetemp=.pytest_tmp` → 169 passed;
  `tests/test_rank.py tests/test_retrieval_pool_target.py tests/test_language_affinity_quota.py` →
  124 passed; `python -m pytest -q --basetemp=.pytest_tmp` → 998 passed, 3 skipped. New
  regressions: tests/test_retrieval_pool_target.py (14 tests — the two rates recorded separately,
  the absolute target persisted and not shrinking on an incremental round, `ceil(40/0.556)=72`, and
  the target not clamped back to 64); tests/test_rank.py (+10 —
  `test_adaptive_screening_deepens_window_when_exclusion_rate_high`,
  `test_shortfall_is_reported_not_padded_with_unscreened_papers`, a `truncated_by_top_k` guard
  where no test in the repo had asserted that field before, four `TestRankNodeBranchedDiagnostics`
  tests covering per-branch filter diagnostics, merged-and-promoted rerank counters, per-batch
  degradation visibility and total-failure recording, and
  `test_filter_reasons_aggregates_by_specific_reason`); tests/test_generation_quality_gate.py (+2 —
  reserve papers excluded from the reference requirement, `uncertain` papers still counted so the
  exclusion does not over-block). Updated: tests/test_slot_extractor.py, where three exact-value
  assertions encoded the old 0.60 ratio (16/72/80 → 18/81/90); each now also states the invariant
  the test actually means, `retrieval_target > required_reference_count`. A direct probe of
  `rank_node` on a 150-per-branch pool with `required_reference_count=40` confirmed
  `ranked=120`, `screening_reserve_k=48`, `screening_reserve_count=96`,
  `rule_selected_count=144`, `truncated_by_top_k=60` and non-empty `zh_filter` / `en_filter`.
- **Convergence review** (required by AGENTS.md, triggered because C1+C2+C3 are repeated
  adjustments to one problem): the review found and fixed one real defect. C3's first
  implementation widened `top_k` to `max_papers + screening_reserve_k` in the branched per-branch
  `deduplicate_and_rank` calls; because `quota = min(minimum, top_k)` and the quota's reserve source
  is `scored[top_k:]`, widening moved the targeted-recovery quota base off the main window, so a
  recovery paper could land near rank 100 and then be cut by the downstream global rerank at
  `top_k = retrieval_target`. Both branches now use `top_k=min(max_papers, len(...))` with
  `reserve_k=screening_reserve_k`, matching the single-branch pattern; pool size is unchanged
  (still 120 by probe) and quota semantics are restored. The two reserve mechanisms are
  intentionally different and carry Chinese `WHY` comments: `reserve_k` on both `rank_node` paths,
  versus a genuinely widened `merge_language_branches(top_k=max_papers + screening_reserve_k)`
  because the merge window must be wide enough to carry both branches' tails.
  `expand_search_year_node`'s `deduplicate_and_rank` deliberately gets no reserve tail: it computes
  `retrieval_requirement_met = len(ranked) >= target`, which a reserve tail would inflate. No
  exploration-era temporary branches, fallbacks or duplicated logic remain, and `filter_reasons` was
  restored rather than deleted once evidence showed it was an orphaned live field rather than dead
  code.
- **Known limitations:** No real CNKI/LLM end-to-end run — every external API, LLM call and
  Selenium path is mocked. The cost of deeper screening (batch count rising from about 6 to at most
  10 = `rerank_candidate_max / rerank_batch_size` = 120/12, roughly 2.5 → 4-5 minutes for that
  stage) is an **estimate, not a measurement**. The production global rerank runs in
  `retrieval_loop.py` (`rank_node` is always called with `llm=None` at `retrieval_loop.py:107,159`
  and `recovery_loop.py:245`), so the promoted `rerank_*` rank-step fields are 0 on the production
  path; that rerank's counters persist through `state["screening_report"]["llm_rerank"]` into the
  `graph.py` result payload. A dedicated `llm_rerank` step was considered and rejected because
  `steps` drives the frontend progress display, making a new step name a UI-visible change beyond
  this plan's scope. The yield is a within-session observation, so the first round has none and
  falls back to the static ≈0.667 assumption; only incremental rounds converge on the measured
  rate. `docs/react_workflow_example.md` still shows an `accepted_before_dedup` field that is no
  longer emitted — pre-existing drift, unrelated to this change. Finally, raising the margin makes
  explicit-count requests retrieve more while C4 removes the silent padding, so sessions that
  previously reported a met requirement will now report `minimum_references_not_met` honestly; that
  is the intended consequence of the decision to report a shortfall rather than pad it with
  unconfirmed papers.

## 2026-09-03 19:37:11 +08:00

- **Modified files:** app/agent/deliverable_router.py, app/agent/writing_plan.py,
  app/agent/nodes/verification.py, tests/test_generation_quality_gate.py,
  docs/quality-gates.md, docs/research-workflow.md, docs/evidence-model.md.
- **Root cause/reason:** The earlier C4 change excluded `rule_screened_reserve` papers
  only in `check_generation_readiness`. A user-authorized best-effort run could continue
  after that gate; `build_writing_plan` then rebuilt and overwrote
  `citation_eligible_paper_ids` from `PaperCard` (which does not carry private screening
  markers), and `citation_check_node` rebuilt its source/count set from every
  `paper_details` item. Those two downstream reconstructions could therefore reintroduce
  an unconfirmed reserve paper and count it toward the explicit reference requirement.
- **Behavior change:** The state-based marker lookup is now the shared public
  `unconfirmed_reference_ids` helper. Generation readiness, WritingPlan construction,
  and final citation source/count construction all consume that same exclusion set.
  Reserve IDs are removed from section support and citation allocation inputs; if a
  best-effort draft nevertheless contains one, citation validation reports it as
  missing, the reference list omits it, `unique_cited_paper_count` excludes it, and the
  final gate reports `minimum_cited_references_not_met` rather than a false success.
  The quality-gate, workflow, and evidence-model documentation now records this
  three-boundary invariant.
- **Tests/validation:** Added
  `test_unconfirmed_reserve_stays_excluded_through_writing_and_final_count`, which
  exercises readiness → WritingPlan → citation validation/counting → final quality gate
  in one state and proves the reserve ID never returns. `tests/test_generation_quality_gate.py`
  → 66 passed; adjacent suites (`test_generation_quality_gate.py`,
  `test_writing_plan.py`, `test_agent_graph.py`, `test_citation.py`,
  `test_retrieval_pool_target.py`) → 179 passed; full suite → 999 passed, 3 skipped.
  `git diff --check` reported no whitespace errors in the touched files.
- **Convergence review:** The root-cause behavior is covered at both bypass points and
  follows the existing readiness pattern. The marker literal remains centralized as
  `RULE_SCREENED_RESERVE`, while state traversal is centralized in one helper; no
  duplicate screening predicates, temporary fallbacks, extra reloads, or scratch files
  were retained. The non-obvious best-effort lifetime boundary has a Chinese `WHY`
  comment, and the regression checks the cross-stage root cause rather than only the
  original pre-generation symptom.
- **Known limitations:** Real CNKI, LLM, and Selenium end-to-end paths were not run;
  external integrations remain mocked. The reserve marker lives in `paper_details` by
  design because `PaperCard` drops private retrieval keys, so a malformed external
  caller that supplies cards without the required detail records cannot reconstruct
  screening provenance and must be treated as incomplete state rather than guessed.
## 2026-09-04 21:30:00 +08:00

- **Modified files:** app/agent/state_invariants.py, app/agent/state.py,
  app/agent/graph.py, app/agent/recovery_loop.py, app/agent/nodes/retrieval.py,
  app/tools/branch_merge.py, tests/test_state_invariants.py,
  tests/test_language_affinity_quota.py, docs/quality-gates.md
- **Root cause/reason:** The plan's core retrieval, citation, recovery, and LLM
  changes were present but state-level consistency and recovery observability were
  not fully connected. Low English screening pass rates could still silently
  transfer quota to Chinese candidates, and stale time/snapshot diagnostics had no
  common write-boundary check.
- **Behavior change:** Added deterministic state-invariant validation at the write
  boundary; blocking time-window, stale-snapshot, and unexplained recovery/readiness
  conflicts now quarantine generation. Added persisted recovery reuse/recompute and
  observed LLM-call counters. Low English pass-rate protection can freeze quota
  transfer so the language shortfall remains visible to the final gate.
- **Tests/validation:** 5 state-invariant tests passed; focused regression tests
  passed after fixing the merge-call compatibility issue. The prior full-suite run
  reached 1009 passed and 3 skipped, with 11 setup errors caused by Windows
  permissions on the default pytest temp directory; a repository-local basetemp run
  is being used for final validation.
- **Known limitations:** Unified compiled scope concepts, fully local section-only
  regeneration, source failure subtype aggregation, and real external LLM/CNKI
  benchmarks remain outside this incremental patch. The state invariant check is
  intentionally deterministic and reports rather than invents missing evidence.

## 2026-09-04 22:17:59 +08:00

- **Modified files:** app/tools/verify_claims.py, app/agent/nodes/verification.py,
  app/agent/graph.py, tests/test_verify_claims.py, tests/test_agent_graph.py
- **Root cause/reason:** Regeneration always rebuilt the claim verification report even when
  the evidence pool and authorization plan were reusable. This caused unchanged sentences
  to be reparsed and revalidated, wasted entailment calls, and made local rewrite recovery
  unable to identify the affected claims from repairs, CCC, or failed-section diagnostics.
- **Behavior change:** `verify_review_claims` and `verify_claims_node` now accept optional
  sentence/claim targets and a verification scope. Local scope reuses the previous report
  for unaffected sentences while retaining the fingerprint entailment cache, emits local
  reuse/recompute statistics, and falls back to full verification when no safe prior report
  or target is available. `regenerate_research_agent` derives local targets from blocking
  issues, claim repairs, claim-citation consistency, and section diagnostics; full rebuilds
  continue to verify every sentence. The final quality gate remains unchanged and still
  controls publication.
- **Tests/validation:** Mock regressions cover target sentence and atomic claim selection,
  prior-report merging, cache reuse, diagnostic target derivation, and local-rewrite routing.
  `python -m pytest tests/test_verify_claims.py tests/test_agent_graph.py -q
  --basetemp=data/_pytest_tmp` passed (74 tests). Python compilation of all modified
  modules passed.
- **Known limitations:** Real LLM/CNKI/Selenium end-to-end paths were not run. Section-only
  target resolution depends on a matching writing-plan title when diagnostics omit sentence
  indices; if no reliable target can be derived, verification safely falls back to full mode.


## 2026-09-04 23:45:00 +08:00

- **Modified files:** app/deliverables/renderers/base_renderer.py, app/schemas/paper_schema.py, app/tools/search_papers.py, app/agent/diagnostics.py, app/agent/evidence_recovery.py, app/frontend/chat_app.py, app/clients/arxiv_client.py, app/clients/openalex_client.py, app/clients/semantic_scholar_client.py, app/clients/crossref_client.py, app/clients/cnki_client.py, tests/test_search.py, tests/test_deliverables.py, tests/test_diagnostics.py.
- **Root cause/reason:** Fallback prose could repeat the same evidence claim, and source diagnostics collapsed language mismatch, empty results, rate limiting, timeouts, and API/authentication failures into the legacy status field. Client-level errors were often returned as empty lists without an auditable subtype.
- **Behavior change:** Fallback claim deduplication now normalizes claim text and keeps section authorization boundaries; same-section duplicate claims merge citation unions, while cross-section claims remain separate. User research wording is not rewritten by paper-claim neutralization. `SourceDiagnostic` retains `status` and adds `outcome`/`failure_category`; search dispatch maps language mismatch, empty/results, rate-limit, timeout, auth, and API outcomes while retaining error codes/messages. Client diagnostic events preserve swallowed failure causes. Diagnostics aggregate outcome counts and affected/language-gap sources, and the frontend safely presents source status and recovery guidance.
- **Tests/validation:** `python -m pytest tests/test_search.py tests/test_dual_channel_retrieval.py tests/test_deliverables.py tests/test_diagnostics.py tests/test_evidence_recovery.py --basetemp=data/_pytest_target2 -q` → 51 passed. `python -m compileall -q app tests` passed. The default pytest temp root has a pre-existing Windows permission error; repository-local basetemp was used.
- **Known limitations:** Real CNKI/LLM/API end-to-end behavior was not run. Existing client return signatures remain list-compatible; clients not emitting an event for a normal empty response are correctly classified as `success_empty` by the dispatcher.


## 2026-09-04 23:30:00 +08:00

- **Modified files:** app/tools/paper_matching.py, app/tools/rank_papers.py, app/tools/language_filter.py, app/agent/nodes/retrieval.py, app/agent/state.py, tests/test_rank.py
- **Root cause/reason:** Scope inputs were interpreted separately by confirmed-scope, semantic-frame, screening-protocol, topic-anchor, branch, and language filtering paths, so initial ranking and post-detail validation could diverge.
- **Behavior change:** Added pure `compile_scope` normalization with stable `compiled_scope.v1` fingerprint, bilingual aliases/tokens, role/source groups, and branch metadata. Scope, topic-anchor, branch, protocol, language, and hard-filter functions accept the compiled snapshot while preserving legacy calls. Rank and detail nodes compile from the same state fields, persist the snapshot and fingerprint in state/diagnostics, and pass it through filtering.
- **Tests/validation:** Python compilation passed. Scope-related targeted tests passed: `python -m pytest tests/test_rank.py tests/test_ranked_rules_enhanced.py tests/test_retrieval_pool_target.py tests/test_dual_channel_retrieval.py -q --basetemp=data/_pytest_scope_final` (134 passed). `git diff --check` passed for the scope files.
- **Known limitations:** No live CNKI/LLM/Selenium run was performed. The broader working tree contains unrelated pre-existing modifications; no Git commit was made.

## 2026-09-04 23:14:30 +08:00

- **Modified files:** app/agent/graph.py, tests/test_agent_graph.py
- **Root cause/reason:** The post-writing verification tail (claim alignment →
  per-sentence claim verification → citation generation/validation →
  claim-citation authorization) was implemented four separate times: the main
  run path, the citation-gap repair sub-path, `continue_research_agent`, and
  `regenerate_research_agent`. Every copy carried its own conditions, so a stage
  could be missing in one entry point only; historically the continue path had
  no claim-citation authorization check and the repair path diverged from the
  first-round chain.
- **Behavior change:** Added `_verify_generated_draft`, one shared chain that
  fixes the stage conditions and order. All four call sites now delegate to it.
  Progress checkpoints stay with each caller through a `checkpoint` callable,
  because step numbering, index clamping, and cancellation granularity differ
  per entry point; the emitted progress sequence is unchanged. The run path
  keeps deferring the authorization check until after citation-gap repair via
  `check_citation_authorization=False`. Local-rewrite verification targets are
  forwarded through `verify_claims_kwargs`. `citation_check_node` is now called
  with `llm=None` everywhere: its `llm` parameter is accepted but unused (the
  citation generation call hardcodes `llm=None`), so the previously inconsistent
  argument had no effect on output.
- **Tests/validation:** Four new regressions pin the chain: fixed stage order
  with no stage missing, deferred authorization for the run path, local target
  forwarding, and per-stage input gating. `python -m pytest
  tests/test_agent_graph.py -q` → 51 passed. Full suite
  `python -m pytest -q --basetemp=data/_pytest_merge_full` → 1039 passed,
  3 skipped. `python -m compileall` and `git diff --check` passed. `pyflakes`
  reports no new unused names (the six pre-existing unused node re-exports in
  `graph.py` were left untouched as unrelated).
- **Convergence review:** The four duplicated copies are gone rather than
  wrapped, no temporary branch or compatibility shim was kept, and the retained
  non-obvious constraints (caller-owned checkpoints, deferred authorization,
  unified `llm=None`) each carry a short Chinese `WHY` comment. Repository-local
  pytest basetemp directories created during validation were removed.
- **Known limitations:** No real CNKI/LLM/Selenium run was performed; this is an
  orchestration refactor validated by mocked regressions only. The claim-plan /
  evidence-gate preamble is still duplicated across the entry points with
  per-caller reuse flags and was deliberately left out of this change.

## 2026-09-04 23:31:44 +08:00

- **Modified files:** .gitignore, README.md, ARCHITECTURE.md, scripts/README.md,
  scripts/check_llm_api.py; removed the tracked `.claude` settings, root-level
  run output and repair-plan files, the obsolete duplicate v1.3 changelog, local
  launch snippets, and one-off incident-diagnostic scripts.
- **Root cause/reason:** The public repository mixed reusable source and operator
  documentation with local Agent configuration, generated task output, private
  repair notes, hard-coded investigation scripts, and redundant release notes.
  This made the GitHub root and `scripts/` directory look like a development dump
  rather than a maintained portfolio project.
- **Behavior change:** Runtime behavior is unchanged. The LLM connectivity check
  moved from the repository root to `scripts/check_llm_api.py`; reusable scripts
  are now documented, and local Agent/IDE/plan artifacts are ignored to prevent
  accidental recommits.
- **Tests/validation:** `python scripts/check_llm_api.py --help` and Python
  compilation passed; repository references to removed filenames returned no
  matches; current-tree and Git-history scans found no `sk-...` secret pattern;
  `git diff --check` passed. The application suite had already passed in this
  workspace snapshot (1039 passed, 3 Selenium-dependent tests skipped) before the
  repository-only cleanup.
- **Known limitations:** Changes are local and have not been committed or pushed.
  Existing modifications to `app/agent/graph.py` and `tests/test_agent_graph.py`
  predate this cleanup and remain intentionally untouched.
