# 2026-09-22 执行、恢复与缓存验收

## 本地验证

- 全量：`python -m pytest -q --basetemp=.tmp-runtime-release`，**1224 passed in 38.08s**。
- `python -m compileall -q app tests scripts/migrate_agent_runtime.py` 通过。
- 常规 `git diff --check` 通过；工作区原有 CRLF 文件在关闭 Git 换行转换的额外检查中
  会被报告为尾部空白，本轮未为消除此报告而整体改写无关文件。
- 受控故障覆盖：两个 worker 同时首次领取、数据库版本竞争、资料写入故障回滚、
  未决请求崩溃恢复、迟到结算、重复终态回调、取消后的公开保存、后台重复投递、
  历史游标、显式恢复保留预算与 40 篇/年份约束，以及大补丁和结构化诊断恢复。

## 真实外部执行

使用隔离验收数据库，未改部署配置、未重启生产服务。主 LLM 密钥当前未配置，
真实请求走已配置备用服务，不能据此声称主服务已验收通过。

完整场景是“近 3 年课堂行为识别的研究背景和研究现状，引用不少于 40 篇”。
首先返回范围澄清；验收补充了计算机视觉为主、兼顾多模态、严格 2024–2026 年的范围。
真实 CNKI 检索两轮分别得到 210 和 250 条窗口内元数据；这些是检索结果数，
不是经过证据审核的可引用论文数。

首轮动作提交暴露 `SourceDiagnostic` 对象无法 JSON 序列化的问题，已改为完整
结构化保存并增加回归。恢复后保留此前 57,380 tokens 消耗，又发生 23,657 tokens，
累计 **81,037 tokens / 100,000 token 上限**。下一请求的保守预留超过剩余额度，
系统明确返回 `blocked`，未发布正文，持久化的篇数要求仍为 40、年份仍为 2024–2026。
验收也发现预算阻断与 `generation_blocked`/任务终态标签不一致，已统一为阻断语义并
补回归；没有为验证该标签再次付费重跑整个场景。

因此，**40 篇完整综述交付尚未验收通过**；没有扩大预算、降低篇数或跳过门禁换取通过。
恢复轮 5 个真实响应记录的缓存 hit/miss 为 3,200 / 12,402，命中率 20.51%。
该轮未运行到最终写作，不能据此评价章节写作的缓存命中率。

另外进行了有界真实提交烟测：原生 LLM 选择检索动作，CNKI 返回 3 篇论文，
Controller 提交成功，关闭并重新打开数据库后恢复的数据完全一致，检查点关联任务为
`completed`。首次烟测曾遇 Chrome 启动失败，保留该次 `failed` 记录；新操作重试成功。
成功轮真实 usage 为 1,405 tokens，其中缓存 hit/miss 为 1,152 / 235（83.06%）。
烟测账本保留两次 LLM 调用累计 2,810 tokens，没有清掉失败尝试的费用。

这些数值只证明真实调用、记账、缓存可见性和提交/恢复链路；不是相同研究质量下的
改动前后节费对照实验。原始结果、数据库和用量报告保存在本地忽略目录
`data/e2e_runs/20260922-runtime-acceptance/`，未放入变更日志或版本控制。
